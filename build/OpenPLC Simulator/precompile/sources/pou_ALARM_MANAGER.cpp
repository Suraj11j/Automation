// Generated by STruC++ - IEC 61131-3 Structured Text to C++ Compiler
// Do not edit this file manually.

#include "generated.hpp"

namespace strucpp {

Program_ALARM_MANAGER::Program_ALARM_MANAGER(GlobalVar<IEC_BOOL>* SAFETY_OK_ref, GlobalVar<IEC_BOOL>* CONVEYOR_FAULT_ref, GlobalVar<IEC_BOOL>* MIXER_FAULT_ref, GlobalVar<IEC_BOOL>* PACKAGING_FAULT_ref, GlobalVar<IEC_BOOL>* PUMP_A_FAULT_ref, GlobalVar<IEC_BOOL>* PUMP_B_FAULT_ref, GlobalVar<IEC_BOOL>* DISCHARGE_PUMP_FAULT_ref, GlobalVar<IEC_BOOL>* INLET_VALVE_FAULT_ref, GlobalVar<IEC_BOOL>* INGREDIENT_A_VALVE_FAULT_ref, GlobalVar<IEC_BOOL>* INGREDIENT_B_VALVE_FAULT_ref, GlobalVar<IEC_BOOL>* DISCHARGE_VALVE_FAULT_ref, GlobalVar<ST_BATCH>* BATCH_ref, GlobalVar<IEC_REAL>* TEMPERATURE_ref, GlobalVar<IEC_BOOL>* CONVEYOR_JAM_ref, GlobalVar<IEC_INT>* BATCH_STATE_CODE_ref, GlobalVar<IEC_BOOL>* SYSTEM_RESET_ref, GlobalVar<IEC_BOOL>* ALARM_ACKNOWLEDGE_ref)
    : ANY_ALARM(false), _TMP_GE3254488_ENO(false), _TMP_GE3254488_OUT(false), _TMP_EQ8926932_ENO(false), _TMP_EQ8926932_OUT(false), SAFETY_OK(SAFETY_OK_ref), CONVEYOR_FAULT(CONVEYOR_FAULT_ref), MIXER_FAULT(MIXER_FAULT_ref), PACKAGING_FAULT(PACKAGING_FAULT_ref), PUMP_A_FAULT(PUMP_A_FAULT_ref), PUMP_B_FAULT(PUMP_B_FAULT_ref), DISCHARGE_PUMP_FAULT(DISCHARGE_PUMP_FAULT_ref), INLET_VALVE_FAULT(INLET_VALVE_FAULT_ref), INGREDIENT_A_VALVE_FAULT(INGREDIENT_A_VALVE_FAULT_ref), INGREDIENT_B_VALVE_FAULT(INGREDIENT_B_VALVE_FAULT_ref), DISCHARGE_VALVE_FAULT(DISCHARGE_VALVE_FAULT_ref), BATCH(BATCH_ref), TEMPERATURE(TEMPERATURE_ref), CONVEYOR_JAM(CONVEYOR_JAM_ref), BATCH_STATE_CODE(BATCH_STATE_CODE_ref), SYSTEM_RESET(SYSTEM_RESET_ref), ALARM_ACKNOWLEDGE(ALARM_ACKNOWLEDGE_ref)
{
}

void Program_ALARM_MANAGER::run() {
    if (NOT(SAFETY_OK->read())) {
        SAFETY_ALARM.ACTIVE = true;
    }
    if (NOT(SAFETY_OK->read())) {
        SAFETY_ALARM.LATCHED = true;
    }
    if (CONVEYOR_FAULT->read()) {
        CONVEYOR_ALARM.ACTIVE = true;
    }
    if (CONVEYOR_FAULT->read()) {
        CONVEYOR_ALARM.LATCHED = true;
    }
    if (PACKAGING_FAULT->read()) {
        PACKAGING_ALARM.ACTIVE = true;
    }
    if (PACKAGING_FAULT->read()) {
        PACKAGING_ALARM.LATCHED = true;
    }
    if (MIXER_FAULT->read()) {
        MIXER_ALARM.ACTIVE = true;
    }
    if (MIXER_FAULT->read()) {
        MIXER_ALARM.LATCHED = true;
    }
    if (PUMP_A_FAULT->read()) {
        PUMP_A_ALARM.ACTIVE = true;
    }
    if (PUMP_A_FAULT->read()) {
        PUMP_A_ALARM.LATCHED = true;
    }
    if (PUMP_B_FAULT->read()) {
        PUMP_B_ALARM.ACTIVE = true;
    }
    if (PUMP_B_FAULT->read()) {
        PUMP_B_ALARM.LATCHED = true;
    }
    if (DISCHARGE_PUMP_FAULT->read()) {
        DISCHARGE_PUMP_ALARM.ACTIVE = true;
    }
    if (DISCHARGE_PUMP_FAULT->read()) {
        DISCHARGE_PUMP_ALARM.LATCHED = true;
    }
    if (INLET_VALVE_FAULT->read()) {
        INLET_VALVE_ALARM.ACTIVE = true;
    }
    if (INLET_VALVE_FAULT->read()) {
        INLET_VALVE_ALARM.LATCHED = true;
    }
    if (INGREDIENT_A_VALVE_FAULT->read()) {
        INGREDIENT_A_VALVE_ALARM.ACTIVE = true;
    }
    if (INGREDIENT_A_VALVE_FAULT->read()) {
        INGREDIENT_A_VALVE_ALARM.LATCHED = true;
    }
    if (INGREDIENT_B_VALVE_FAULT->read()) {
        INGREDIENT_B_VALVE_ALARM.ACTIVE = true;
    }
    if (INGREDIENT_B_VALVE_FAULT->read()) {
        PUMP_B_ALARM.LATCHED = true;
    }
    if (DISCHARGE_VALVE_FAULT->read()) {
        DISCHARGE_VALVE_ALARM.ACTIVE = true;
    }
    if (DISCHARGE_VALVE_FAULT->read()) {
        DISCHARGE_VALVE_ALARM.LATCHED = true;
    }
    if (CONVEYOR_JAM->read()) {
        CONVEYOR_JAM_ALARM.ACTIVE = true;
    }
    if (CONVEYOR_JAM->read()) {
        CONVEYOR_JAM_ALARM.LATCHED = true;
    }
    if (true) {
        _TMP_GE3254488_OUT = GE(TEMPERATURE->read(), BATCH->with_lock([&](auto* __glk){ return (*__glk).TEMPERATURE_SP; }));
        _TMP_GE3254488_ENO = true;
    } else {
        _TMP_GE3254488_ENO = false;
    }
    if (_TMP_GE3254488_OUT) {
        TEMPERATURE_ALARM.ACTIVE = true;
    }
    if (true) {
        _TMP_EQ8926932_OUT = EQ(BATCH_STATE_CODE->read(), static_cast<IEC_INT>(7));
        _TMP_EQ8926932_ENO = true;
    } else {
        _TMP_EQ8926932_ENO = false;
    }
    if (_TMP_EQ8926932_ENO) {
        BATCH_FAULT_ALARM.ACTIVE = _TMP_EQ8926932_OUT;
    }
    ANY_ALARM = (((((((((((((BATCH_FAULT_ALARM.ACTIVE) | (CONVEYOR_ALARM.ACTIVE)) | (CONVEYOR_JAM_ALARM.ACTIVE)) | (DISCHARGE_PUMP_ALARM.ACTIVE)) | (DISCHARGE_VALVE_ALARM.ACTIVE)) | (INGREDIENT_A_VALVE_ALARM.ACTIVE)) | (INGREDIENT_B_VALVE_ALARM.ACTIVE)) | (INLET_VALVE_ALARM.ACTIVE)) | (MIXER_ALARM.ACTIVE)) | (PACKAGING_ALARM.ACTIVE)) | (PUMP_A_ALARM.ACTIVE)) | (PUMP_B_ALARM.ACTIVE)) | (SAFETY_ALARM.ACTIVE)) | (TEMPERATURE_ALARM.ACTIVE);
    if (ALARM_ACKNOWLEDGE->read()) {
        SAFETY_ALARM.ACKNOWLEDGED = true;
    }
    if (ALARM_ACKNOWLEDGE->read()) {
        CONVEYOR_ALARM.ACKNOWLEDGED = true;
    }
    if (ALARM_ACKNOWLEDGE->read()) {
        PACKAGING_ALARM.ACKNOWLEDGED = true;
    }
    if (ALARM_ACKNOWLEDGE->read()) {
        MIXER_ALARM.ACKNOWLEDGED = true;
    }
    if (ALARM_ACKNOWLEDGE->read()) {
        PUMP_A_ALARM.ACKNOWLEDGED = true;
    }
    if (ALARM_ACKNOWLEDGE->read()) {
        PUMP_B_ALARM.ACKNOWLEDGED = true;
    }
    if (ALARM_ACKNOWLEDGE->read()) {
        DISCHARGE_PUMP_ALARM.ACKNOWLEDGED = true;
    }
    if (ALARM_ACKNOWLEDGE->read()) {
        INLET_VALVE_ALARM.ACKNOWLEDGED = true;
    }
    if (ALARM_ACKNOWLEDGE->read()) {
        INGREDIENT_A_VALVE_FAULT->write(true);
    }
    if (ALARM_ACKNOWLEDGE->read()) {
        INGREDIENT_B_VALVE_FAULT->write(true);
    }
    if (ALARM_ACKNOWLEDGE->read()) {
        DISCHARGE_VALVE_ALARM.ACKNOWLEDGED = true;
    }
    if (ALARM_ACKNOWLEDGE->read()) {
        BATCH_FAULT_ALARM.ACKNOWLEDGED = true;
    }
    if (ALARM_ACKNOWLEDGE->read()) {
        CONVEYOR_JAM_ALARM.ACKNOWLEDGED = true;
        TEMPERATURE_ALARM.ACKNOWLEDGED = true;
    }
    if (SYSTEM_RESET->read()) {
        SAFETY_ALARM.ACKNOWLEDGED = false;
    }
    if (SYSTEM_RESET->read()) {
        SAFETY_ALARM.LATCHED = false;
    }
    if (SYSTEM_RESET->read()) {
        CONVEYOR_ALARM.ACKNOWLEDGED = false;
    }
    if (SYSTEM_RESET->read()) {
        CONVEYOR_ALARM.LATCHED = false;
    }
    if (SYSTEM_RESET->read()) {
        PACKAGING_ALARM.ACKNOWLEDGED = false;
    }
    if (SYSTEM_RESET->read()) {
        PACKAGING_ALARM.LATCHED = false;
    }
    if (SYSTEM_RESET->read()) {
        MIXER_ALARM.ACKNOWLEDGED = false;
    }
    if (SYSTEM_RESET->read()) {
        MIXER_ALARM.LATCHED = false;
    }
    if (SYSTEM_RESET->read()) {
        PUMP_A_ALARM.ACKNOWLEDGED = false;
    }
    if (SYSTEM_RESET->read()) {
        PUMP_A_ALARM.LATCHED = false;
    }
    if (SYSTEM_RESET->read()) {
        PUMP_B_ALARM.ACKNOWLEDGED = false;
    }
    if (SYSTEM_RESET->read()) {
        PUMP_B_ALARM.LATCHED = false;
    }
    if (SYSTEM_RESET->read()) {
        DISCHARGE_PUMP_ALARM.ACKNOWLEDGED = false;
    }
    if (SYSTEM_RESET->read()) {
        DISCHARGE_PUMP_ALARM.LATCHED = false;
    }
    if (SYSTEM_RESET->read()) {
        INLET_VALVE_ALARM.ACKNOWLEDGED = false;
    }
    if (SYSTEM_RESET->read()) {
        INLET_VALVE_ALARM.LATCHED = false;
    }
    if (SYSTEM_RESET->read()) {
        INGREDIENT_A_VALVE_FAULT->write(false);
    }
    if (SYSTEM_RESET->read()) {
        INGREDIENT_A_VALVE_ALARM.LATCHED = false;
    }
    if (SYSTEM_RESET->read()) {
        INGREDIENT_B_VALVE_FAULT->write(false);
    }
    if (SYSTEM_RESET->read()) {
        INGREDIENT_B_VALVE_ALARM.LATCHED = false;
    }
    if (SYSTEM_RESET->read()) {
        DISCHARGE_VALVE_ALARM.ACKNOWLEDGED = false;
    }
    if (SYSTEM_RESET->read()) {
        DISCHARGE_VALVE_ALARM.LATCHED = false;
    }
    if (SYSTEM_RESET->read()) {
        BATCH_FAULT_ALARM.ACKNOWLEDGED = false;
    }
    if (SYSTEM_RESET->read()) {
        BATCH_FAULT_ALARM.LATCHED = false;
    }
    if (SYSTEM_RESET->read()) {
        CONVEYOR_JAM_ALARM.ACKNOWLEDGED = false;
        TEMPERATURE_ALARM.ACTIVE = false;
    }
    if (SYSTEM_RESET->read()) {
        CONVEYOR_JAM_ALARM.LATCHED = false;
    }
    if (SYSTEM_RESET->read()) {
        TEMPERATURE_ALARM.LATCHED = false;
    }
}

}  // namespace strucpp